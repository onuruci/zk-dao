import React from "react";
import { observer } from 'mobx-react-lite'
import './proposals.css'

export default observer(() => {
    return(
        <div className="proposal-container">
            Governance will appear here!
        </div>
    )
 })